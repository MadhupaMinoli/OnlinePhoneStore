import { MobilePhoneCollection } from './mobile-phone-collection';

describe('MobilePhoneCollection', () => {
  it('should create an instance', () => {
    expect(new MobilePhoneCollection()).toBeTruthy();
  });
});
